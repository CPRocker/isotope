// This is a single line comment

/*
    This is a block comment
*/

let x = 10;
let y = 11;
let z = 21;
let a = 32;
let b = !(1 < 32) >= 5;
return y + z / x.foo[10];

loop {
    if(b < 7) {
        c = 1;
    } else if(b > 9) {
        c = 2;
    } else {
        c = 3;
        break;
    }
}

if(a < 10) 
{
    break;
} 
else 
{
    a = 10;
}

fn foo(bar, baz, cat, dog) {
    let a = 4;
    return a;
}

return foo(x.bar, y, z + 142 - 12 * 2, a[10]);
