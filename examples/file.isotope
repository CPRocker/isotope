// This is a single line comment

/*
    This is a block comment
*/

let x = 10;
let y = 11;
let z = 21;
let a = 32;
let b = !(1 < 32) >= 5;
ret y + z / x.foo[10];

fun foo(bar, baz, cat, dog) {
    let a = 4;
    ret a;
}

ret foo(x, y, z, a);
